---
description: 生产机与 Windows 开发/测试机双环境对齐与同步约定
alwaysApply: true
---

# 双环境：生产 ↔ 开发/测试

## 默认模式：生产优先

用户**优先在生产机改**（本工作区 `/opt/jizhuangxiang`）。Agent 默认：

1. 在生产改源码 / 跑幂等迁移
2. 改完 `commit` + `push` 到 `origin/main`
3. Windows 开发机需要测时再 `git pull`（用户连上开发机会话时 Agent 拉齐）

只有用户明确说「先在开发机改 / 先测再上生产」时，才反过来。

## 环境对照

| 角色 | 路径 | 系统 | 数据库 |
|------|------|------|--------|
| **生产（默认改这里）** | `/opt/jizhuangxiang` | Linux CentOS，Next `:3000`，MySQL 8 | 真实业务数据 |
| **开发/测试** | `D:\V0 projects\jizhuangxiang` | Windows + 本地 MySQL 8 | 独立库，可大胆测 |

## 同步什么 / 不同步什么

**要同步（git）：** 源码、`scripts/`、`scripts/sql/`、schema 迁移、`.cursor/rules/`、依赖清单  

**永不同步：** `.env*.local`、密钥、业务数据 dump、`node_modules`、`.next`、上传附件实体

## 生产优先时的注意点

- 改 UI/逻辑：可直接改生产；改完备份推送。
- **破坏性库操作**（删列、改类型、清表）：先说明风险；能写成幂等迁移再执行；必要时先在 Windows 库试。
- 临时调试代码、只给自己看的实验：**不要**推进 `main`；用完删掉或留在未跟踪文件。
- 生产 `.env*.local` 动过变量名时：同步更新 `.env.example`（占位符），方便开发机补齐。

## 数据库

- 默认只同步 **schema / 迁移脚本**，业务数据两边独立。
- 迁移必须幂等；生产优先时可先在生产执行，再靠 git 让开发机跟上。
- 禁止未经确认的整库 dump 互盖。

## 生产机 Git

- 二进制：`/usr/local/bin/git`
- Remote：`git@github.com-jizhuangxiang:liuyangexe2000/jizhuangxiang.git`（Deploy Key）
- `safe.directory` 已配；源码 **LF**（`.gitattributes`）
- `.env.example` 只放占位符

## Shell

- 生产：bash heredoc（见 `agent-owns-git-backup`）
- Windows：PowerShell here-string
